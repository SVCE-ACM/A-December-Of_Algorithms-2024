function findMissingNumber(N, array) {
    const expectedSum = (N * (N + 1)) / 2;
    const actualSum = array.reduce((acc, num) => acc + num, 0);
    return expectedSum - actualSum;
}
console.log(findMissingNumber(5, [1, 2, 4, 5]));
console.log(findMissingNumber(3, [1, 3]));     
